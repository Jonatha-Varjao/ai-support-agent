# Frontend feature flags
# Set to "true" to use a local mock LLM stream instead of the real /chat backend endpoint.
VITE_USE_MOCK_LLM=false

# API base URL — leave empty for Vite proxy (dev) or set to the API origin in production.
VITE_API_BASE_URL=
