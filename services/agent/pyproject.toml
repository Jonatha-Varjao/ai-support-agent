[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=3.1.3",
    "databricks-agents>=1.1.0",
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langchain-google-genai>=2",
    "pyyaml>=6",
]
