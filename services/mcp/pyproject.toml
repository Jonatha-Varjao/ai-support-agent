[project]
name = "mcp-server"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4",
    "playwright>=1.60",
    "markdownify>=0",
    "uvicorn>=0.49",
]

[project.optional-dependencies]
test = [
    "pytest>=9",
]
